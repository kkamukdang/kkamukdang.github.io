# P0 최종 코드검증 인수결과

검증일: 2026-09-10  
대상 저장소: `https://github.com/kkamukdang/kkamukdang.github.io`  
대상 브랜치: `feature/learning-v2`  
최종 판정: **PASS WITH NOTES**

## 1. Git 기준과 시작 상태

| 항목 | 확인값 |
|---|---|
| 시작 branch | `feature/learning-v2` |
| 시작 working tree | clean |
| 시작 local HEAD | `55fdc9ac8caf5623e8772eb8daa6fd68105a9bb6` |
| 시작 origin feature HEAD | `55fdc9ac8caf5623e8772eb8daa6fd68105a9bb6` |
| origin/main HEAD | `2f296327d724f9a085dc470bd3feaed5725b50c1` |
| 시작 `origin/main...HEAD` | main 전용 0 / feature 전용 1 |
| origin URL | `https://github.com/kkamukdang/kkamukdang.github.io.git` |
| 원격 feature 존재 | yes |

기존 문서의 구현 SHA `0db699c34ee1c520f6e2d5bf3e03ec50c48b99fd` 객체는 로컬에 남아 있으나 원격 branch history에는 없다. 터미널 인증이 분리된 환경에서 GitHub API로 동일 P0 소스 트리를 원격 반영하면서 `55fdc9a` 단일 커밋으로 재작성됐기 때문이다. 실제 소스 동등성은 원격 tree 반영과 이후 fetch/rebase로 확인했다. `main`은 변경하거나 merge하지 않았다.

## 2. 실제 코드 기반 P0 대조

| 검증 영역 | 판정 | 실제 코드 확인 |
|---|---|---|
| Git / route | PASS | feature에 구현 존재, main 미반영, 백업 route 삭제, 정상 Episode route 유지 |
| TypeScript 상태 타입 | PASS | v2 ID·시간·stage·rating·상태·event·receipt·command/result 타입 구현, UI 비의존 |
| KST DateOnly | PASS | `Asia/Seoul`, KST 자정·월말·연말·허용 간격·rollback 구현 및 테스트 |
| StorageAdapter / v2 | PASS | 어댑터 분리, runtime validation, revision+receipt+핵심 mutation 재검증, 1회 재시도 후 conflict |
| v1 → v2 migration | PASS | key/snapshot/fingerprint/backup/marker/검증/복구/중복 방지, 원본 및 retired ID 보존 |
| Pure transition | PASS | episode·unsure·평가·졸업·재활성화·newsletter·season 로직이 UI와 분리 |
| 일정 정책 | PASS | 최초 +3, unsure +1, remembered +7/+14/졸업, fuzzy +5, unfamiliar +2 |
| History / receipt | PASS | lifecycle history 보존, 일반 review soft cap, receipt 별도 영속·payload idempotency |
| Queue / reviewFlow | PASS | active due만 선택, due→위험도→최근복습→episode→ID 정렬, daily 3개와 완료 후 extra |
| Episode schema | PASS WITH NOTE | 호환용 optional 필드와 strict resolver가 준비됨. 실제 원고 전체 보강은 Work 2에서 수행 |
| Expression Registry | PASS | YAML 단일 원천, active 18·retired 1, 정규화·중복 검사, queue에서 difficulty 미사용 |
| 공개 서비스 API | PASS | 화면이 재사용할 service/storage/date/transition/queue export 제공 |

## 3. 보강 수정과 회귀 테스트

최종 코드 검증에서 확인된 P0 결함은 `db25dce`에서 수정했다.

- 핵심 mutation 상태의 저장 후 동등성 검증
- `/again/` 평가와 reviewFlow 응답의 원자적 저장
- active batch 완료 전 extra batch 생성 차단
- daily batch에만 `baseCompletedAt` 기록
- migration marker를 이용한 동일 fingerprint 중복 실행 차단
- snapshot 읽기 실패 시 빈 상태 fallback 금지
- migration 성공 후 v1 원본 key 유지
- 중첩 상태 runtime validation 강화
- UTC Instant command 검증 강화

추가 회귀 테스트는 저장 핵심 상태 변조 conflict, reviewFlow 동시 갱신, extra batch gate, marker 중복 방지를 포함한다.

## 4. 명령 실행 결과

| 명령 | 종료 코드 | 결과 |
|---|---:|---|
| `npm run check` | 0 | 콘텐츠 구조 오류 0, Astro sync, `tsc --noEmit` 통과 |
| `npm run test` | 0 | Test Files 7 passed, Tests 28 passed |
| `npm run build` | 0 | 18 pages built |

Route 산출물:

- Episode 읽기 route: 6개
- Episode 쓰기 route: 6개
- `_bak` 소스/산출물: 0개
- 기존 `/again/`, archive, cards, review, JSON endpoint 유지

비차단 warning:

- 기존 콘텐츠 다독음 발음 확인 4건
- 실행 환경의 npm `http-proxy` 설정 deprecation warning
- OG 이미지 생성 시 Noto Sans JP/KR 폰트를 CDN에서 정상 로드

## 5. 최종 판정과 다음 단계

**PASS WITH NOTES**로 인수한다.

Notes는 P0 실패가 아니다.

1. 기존 구현 SHA `0db699c`는 원격 history에 직접 존재하지 않지만 동일 소스가 `55fdc9a`로 반영됐다.
2. 현재 사용자 UI의 v2 엔진 연결, #001 콘텐츠 보강, 모바일·키보드·screen reader 수동 검증은 요청대로 Work 2에 남겼다.
3. 기존 콘텐츠 warning 4건은 오류가 아니다.

따라서 P0 엔진을 기준선으로 고정하고 **Work 2 — #001 세로 절편 구현을 시작할 수 있다.** 이번 인수 작업에서는 Work 2 코드를 수정하지 않았다.
