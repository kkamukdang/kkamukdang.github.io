# P0 기반 엔진 구현 결과·인수 체크 v1

- 작업 브랜치: `feature/learning-v2`
- 안정 기준: `origin/main` `2f296327d724f9a085dc470bd3feaed5725b50c1`
- 원격 최초 P0 반영 커밋: `55fdc9ac8caf5623e8772eb8daa6fd68105a9bb6`
- 최종 엔진 보강 커밋: `5dbd0e34cbb6b9393ca674f64091b1e87087da86` (`fix: harden P0 learning engine acceptance contracts`)
- 최종 판정: **PASS WITH NOTES**
- 다음 Work: `Work 2 — #001 세로 절편 구현` 진행 가능

## 1. 구현 범위

| 영역 | 구현 결과 |
|---|---|
| 상태 모델 | v2 상태·command·result·event·receipt 타입과 runtime validation |
| 시간 | `Asia/Seoul` DateOnly, 달력 일수, due, clock rollback 방어 |
| 저장 | 플랫폼 독립 `StorageAdapter`, localStorage 어댑터, revision/receipt/핵심 상태 저장 후 검증, 최대 1회 재시도 |
| migration | v1 snapshot/fingerprint/backup/marker, validation, retired ID 보존, 실패 시 원본 유지, 동일 fingerprint 중복 방지 |
| 전이 | episode 완료, 자신 없어요, 3단계 평가, 졸업, 재활성화, newsletter·season 전이 |
| 복습 | Registry active 필터, due 우선순위, daily/extra batch, 같은 날 flow 복원과 응답 진행도 |
| 기록 | receipt 기반 idempotency와 lifecycle 보존형 history soft cap |
| 콘텐츠 | Episode 학습 타입, 단일 YAML Registry, active/retired index, stage별 prompt resolver |
| 공개 API | `src/lib/learning/index.ts`를 통한 다음 Work용 진입점 |

삭제한 백업 route:

- `src/pages/ep/[slug]_bak.astro`
- `src/pages/ep/[slug]_bak_0901.astro`

## 2. 최종 재검증에서 보강한 내용

1. 서비스 mutation 저장 후 검증을 레코드 존재 여부에서 실제 expression·episode·season·reviewFlow 기대 상태의 동등성 검증으로 강화했다.
2. `again` 평가와 active review batch의 `answeredIds`/`baseCompletedAt` 갱신을 하나의 저장 mutation으로 묶었다.
3. active batch 완료 전 extra batch를 만들지 않으며, 기본 완료 시각은 daily batch 완료에만 기록하도록 했다.
4. v1 snapshot 읽기 실패를 빈 상태로 오인하지 않게 했고, 완료 marker와 동일 fingerprint면 중복 migration을 차단했다.
5. migration 성공 후에도 v1 원본 key를 유지하고 backup과 v2를 함께 보존한다.
6. season response, reviewFlow batch, receipt, history 참조 ID, migration counter의 runtime validation을 강화했다.
7. command의 `now`는 임의의 parse 가능 문자열이 아니라 UTC `Z` Instant만 허용한다.

## 3. 검증 결과

| 명령/검사 | 종료 코드 | 결과 |
|---|---:|---|
| `npm run check` | 0 | 콘텐츠 오류 0, Astro sync 및 TypeScript 통과 |
| `npm run test` | 0 | 7개 파일, 28개 테스트 통과 |
| `npm run build` | 0 | 정적 18페이지 빌드 통과 |
| backup route 검사 | 0 | 소스·`dist` 모두 `_bak` 0개 |
| 정상 Episode route | - | 읽기 6개·쓰기 6개 유지 |

콘텐츠 검사에는 기존 원고의 다독음 발음 확인 warning 4건(`明日` 2건, `行` 2건)이 남아 있다. 오류가 아니며 exit code는 0이다. npm은 실행 환경의 기존 `http-proxy` 환경설정에 대한 향후 호환 warning을 출력했지만 검사·테스트·빌드에는 영향을 주지 않았다.

## 4. Work 2 인수사항

```ts
import {
  LearningService,
  LocalStorageAdapter,
  getKstDate,
} from '../lib/learning';
```

- 브라우저 controller에서만 `window.localStorage`를 어댑터에 주입한다.
- 화면에서 날짜·stage·streak·졸업을 직접 계산하거나 v2 key에 직접 접근하지 않는다.
- mutation은 `applied`일 때 성공 UI를 표시하고, `duplicate`는 기존 결과를 복원하며, `no-change`는 상태를 바꾸지 않는다.
- newsletter preview는 `migrate({ persist:false })`를 사용해 write 0회를 유지한다.
- `eventId`는 한 사용자 행동 동안 안정적으로 재사용한다.
- `s01e04-maniau` 상태를 `s01e04-madaikeru`로 옮기지 않는다.
- queue는 Registry active만 포함하며 `difficulty`를 정렬에 사용하지 않는다.

UI는 아직 `public/kkmd.js`의 v1 흐름을 사용한다. P0 엔진 범위에서 의도적으로 유지했으며, #001 화면 연결·접근성·360px 수동 검증은 Work 2 범위다.
