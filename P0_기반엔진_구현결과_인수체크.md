# P0 기반 엔진 구현 결과·인수 체크

- 작업 브랜치: `feature/learning-v2`
- 시작 기준 커밋: `2f296327d724f9a085dc470bd3feaed5725b50c1`
- 기반 엔진 구현 커밋: `0db699c34ee1c520f6e2d5bf3e03ec50c48b99fd`
- `main`과 작업 시작 시점 차이: 없음 (`0 / 0`)
- 다음 Work: `Work 2 — #001 세로 절편 구현`

## A. 구현 결과 요약

### 삭제 파일

- `src/pages/ep/[slug]_bak.astro`
- `src/pages/ep/[slug]_bak_0901.astro`

### 핵심 신규 모듈

| 경로 | 책임 |
|---|---|
| `src/lib/learning/types.ts` | v2 상태, event, receipt, command/result 타입 |
| `src/lib/learning/date-kst.ts` | KST DateOnly, 달력 일수 계산, due, clock rollback |
| `src/lib/learning/validation.ts` | v2 상태 runtime validation과 prototype pollution key 거부 |
| `src/lib/learning/storage.ts` | 플랫폼 독립 `StorageAdapter` 계약 |
| `src/lib/learning/local-storage.ts` | v2 key 접근, 저장 후 검증, 1회 재시도, 손상 raw 보존 |
| `src/lib/learning/migration-v1.ts` | v1 snapshot/fingerprint/변환과 retired ID 보존 |
| `src/lib/learning/transitions.ts` | episode, unsure, 평가, 졸업, 재활성화, 시즌 전이 |
| `src/lib/learning/history.ts` | 1,000건 soft cap과 lifecycle 보존 |
| `src/lib/learning/queue.ts` | due 정렬, daily/extra batch, reviewFlow 복원 |
| `src/lib/learning/review-content.ts` | R1/R2/R3 stage별 prompt resolver |
| `src/lib/learning/service.ts` | receipt/idempotency와 검증된 read-modify-write API |
| `src/lib/learning/index.ts` | 다음 Work용 공개 export 진입점 |
| `src/lib/content/episode-types.ts` | 화면과 독립된 Episode 학습 콘텐츠 타입 |
| `src/lib/content/expression-registry.ts` | Registry loader, normalization, validation, index |
| `src/data/expressions/season-01.yaml` | Season 1 active 18개와 retired `s01e04-maniau` Registry |

### 수정 파일

- `src/content.config.ts`: `quizIndex`, `memoryCue`, stage별 `reviewPrompt.R2/R3` 스키마 추가
- `src/pages/expressions.json.ts`: Registry loader/index 결합 및 공개 메타데이터 추가
- `scripts/check.mjs`: 구조 오류가 있을 때 exit code 1 반환
- `package.json`, `package-lock.json`: Vitest·TypeScript 검사와 build gate 추가

## B. 테스트·빌드 결과

| 명령 | 결과 |
|---|---|
| `npm run check` | 통과 — 콘텐츠 구조 검사, Astro sync, `tsc --noEmit` |
| `npm run test` | 통과 — 7개 파일, 25개 테스트 |
| `npm run build` | 통과 — 18 pages |
| 백업 route 검사 | 통과 — 소스·새 빌드 산출물 `_bak` 0개 |
| 정상 route 검사 | 통과 — Episode 6개, 쓰기 route 6개 및 기존 주요 route 유지 |

자동 검증 범위:

- KST 자정, 월말·연말, +1/+2/+3/+5/+7/+14, clock rollback
- 최초 episode 등록, unsure overwrite 방지
- R1/R2/R3 remembered, 졸업, fuzzy, unfamiliar, 재활성화
- daily 3개, extra batch, 같은 날 reviewFlow 복원
- v1 migration, retired 보존, migration 실패, persist:false 무저장
- stage별 prompt와 R2/R3 교차 fallback 금지
- revision/receipt/core 저장 후 검증과 conflict
- history soft cap, history/receipt 역할 분리, idempotency
- Registry loader·정규화·active/retired index

기존 원고의 다독음 발음 확인 4건은 오류가 아닌 기존 warning으로 남아 있다.

## C. 개발명세 v2 대비 차이

### 의도적으로 다음 Work에 남긴 부분

- 기존 UI는 아직 `public/kkmd.js`의 v1 호출을 사용한다. 이번 Work 요청이 기반 엔진까지였으므로 기존 화면 저장 호출을 교체하지 않았다.
- Episode 실제 원고의 `memoryCue`, R2/R3 prompt 전체 보완은 하지 않았다. 현재 #002~#006 원고를 깨지 않도록 Astro collection 필드는 호환용 optional로 추가했고, strict resolver 검증은 `validateEpisodeReviewContent()`에 분리했다.
- #001 실제 화면 플로우, newsletter 확인 화면, archive 재활성화 UI, 시즌 총복습 UI는 구현하지 않았다.

학습 일정·졸업·migration·receipt·history·queue 정책 자체의 명세 변경은 없다.

## D. Work 2 인수사항

### 사용할 API

```ts
import {
  LearningService,
  LocalStorageAdapter,
  getKstDate,
} from '../lib/learning';
```

브라우저 controller에서만 `window.localStorage`를 `LocalStorageAdapter`에 주입한다. 화면에서 날짜·stage·streak·졸업을 직접 계산하거나 v2 key에 직접 접근하지 않는다.

주요 서비스 메서드:

- `completeEpisode(command)`
- `markUnsure(command)`
- `rateReview(command)`
- `reactivate(command)`
- `answerSeasonReview(command)`
- `completeSeason(command)`
- `getOrCreateReviewFlow(today)`
- `createExtraBatch(today)`
- `getSnapshot()`
- `previewMigration()`

UI는 mutation 결과가 `{ ok:true, status:'applied' }`일 때만 성공 상태를 표시한다. `duplicate`는 기존 결과를 재구성하고, `no-change`는 상태를 바꾸지 않는다.

### #001에 남아 있는 작업

1. #001 keyPoint 3개에 `memoryCue`와 필요한 R3 데이터를 추가한다.
2. episode 완료·`자신 없어요` controller를 새 service에 연결한다.
3. `/again/`을 Registry/reviewFlow/prompt resolver 기반 controller에 연결한다.
4. 피드백 확인 후 사용자가 `다음 표현`을 눌러 이동하도록 구성한다.
5. 졸업 후 archive 확인·재활성화 흐름을 연결한다.
6. 저장 실패·conflict·읽기 전용 문구와 접근성 focus를 연결한다.
7. 360px, 키보드, screen reader로 #001 완료→R1→R2→R3→졸업→재활성화를 수동 검증한다.

### 기술적 주의점

- newsletter preview에서는 `migrate({ persist:false })`를 사용하며 localStorage write가 0회여야 한다.
- `eventId`는 한 사용자 행동 동안 안정적으로 재사용해야 한다. 같은 ID+같은 payload는 `duplicate`, 같은 ID+다른 payload는 `invalid-command`다.
- `s01e04-maniau` 상태를 `s01e04-madaikeru`로 옮기지 않는다.
- queue는 Registry active만 포함하며 `difficulty`를 정렬에 사용하지 않는다.
- build-time Registry 원천은 `src/data/expressions/season-01.yaml` 하나다.

### 테스트 fixture 위치

- `tests/helpers.ts`
- `tests/learning/date-kst.test.ts`
- `tests/learning/transitions.test.ts`
- `tests/learning/queue.test.ts`
- `tests/learning/migration-v1.test.ts`
- `tests/learning/review-content.test.ts`
- `tests/learning/storage-service.test.ts`
- `tests/learning/registry.test.ts`
