{
  "name": "kkamukdang",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check.mjs && astro build && pagefind --site dist",
    "preview": "astro preview",
    "check": "node scripts/check.mjs"
  },
  "dependencies": {
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "pagefind": "^1.3.0",
    "js-yaml": "^4.1.0"
  }
}